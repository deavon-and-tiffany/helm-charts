apiVersion: scheduling.k8s.io/v1
kind: PriorityClass

metadata:
  name: low-priority
value: 100
globalDefault: false
description: "Pods with this priority class will be the most likely to be evicted and rescheduled during a scale event."
